# 个人博客

基于 Hugo 和 Ladder 主题构建的个人博客网站。

## 功能特性

- ✨ 支持暗黑模式和明亮模式切换
- 🌍 多语言支持（中英文）
- 📱 响应式设计，移动端友好
- 🎯 专注于阅读体验的简洁设计
- 🏷️ 标签和归档功能
- 💬 留言板功能

## 本地开发

### 前置要求

- [Hugo Extended](https://gohugo.io/installation/) (推荐版本 0.99.0 或更高)

### 安装步骤

1. 克隆仓库：
```bash
git clone https://github.com/bryanleungds/bryanleungds.github.io.git
cd bryanleungds.github.io
```

2. 初始化子模块（主题）：
```bash
git submodule update --init --recursive
```

3. 启动本地开发服务器：
```bash
hugo server -D
```

4. 在浏览器中访问 `http://localhost:1313`

## 部署

### 自动部署（推荐）

本项目已配置 GitHub Actions，当你推送代码到 `main` 或 `master` 分支时，会自动构建并部署到 GitHub Pages。

### 手动部署

1. 构建站点：
```bash
hugo --minify
```

2. 构建产物在 `public/` 目录，可以：
   - 推送到 `gh-pages` 分支
   - 或使用其他静态网站托管服务

## 配置说明

主要配置文件：`config.yml`

### 重要配置项

- `baseURL`: 你的网站地址
- `title`: 网站标题
- `Author.name`: 作者名称
- `Author.email`: 作者邮箱
- `params.author`: 显示在首页的作者名
- `params.authorDescription`: 作者简介
- `params.social`: 社交媒体链接

## 添加文章

在 `content/blog/` 目录下创建 Markdown 文件，使用以下 front matter：

```markdown
---
title: "文章标题"
date: 2024-01-01T10:00:00+08:00
tags: ["标签1", "标签2"]
draft: false
featured: true  # 可选：是否在首页展示
---

文章内容...
```

## 目录结构

```
.
├── archetypes/          # Hugo 内容模板
├── content/             # 网站内容
│   ├── blog/           # 博客文章
│   ├── art/            # 生活/艺术内容
│   └── ...
├── static/              # 静态资源（图片、CSS等）
├── themes/              # Hugo 主题
├── config.yml           # 站点配置文件
└── .github/workflows/   # GitHub Actions 工作流
```

## 许可证

MIT License

## 致谢

- [Hugo](https://gohugo.io/) - 静态站点生成器
- [Ladder Theme](https://github.com/guangzhengli/hugo-theme-ladder) - Hugo 主题
